function resultsConverter(inputObj) {
  let inputArr = inputObj && inputObj.hits.hits;
  if (inputArr.length > 0) {
    const results = inputArr.map(({ _source }) => _source);
    return results;
  }
  return inputArr;
}

module.exports = {
  resultsConverter
};
