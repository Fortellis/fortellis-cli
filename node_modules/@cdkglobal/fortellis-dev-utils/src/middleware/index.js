const localApiGateway = require('./localApiGateway');
const entitlementsProcessor = require('./entitlementsProcessor');
const trafficLogger = require('./trafficLogger');

module.exports = {
  localApiGateway,
  entitlementsProcessor,
  trafficLogger
};
