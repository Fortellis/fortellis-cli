import { IRuleResult } from '../../types';
import { OutputFormat } from '../../types/config';
export declare function formatOutput(results: IRuleResult[], format: OutputFormat): string;
export declare function writeOutput(outputStr: string, outputFile?: string): Promise<void>;
